
<div align = 'center'> <h1> BruGram's Home </h1> </div>
<% @photos.each do |f|%>
	<div align='center'><p><%= link_to image_tag(f.image.url(:medium)), photo_path(f) %></p>
	<p><%= pluralize(f.vote, "vote") %></p>
	<p><%= button_to '+1', upvote_photo_path(f), method: :post %></p>
	</div>
<%end%>